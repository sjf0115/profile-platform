# 标签创建和修改功能逻辑优化

## 已完成（用户手动变更）

- `create()` 已使用 `LabelSourceType.DATASET.getCode()` 替代硬编码
- `create()` 已实现状态自动管理：非数据集类型→ENABLED，数据集+跳过→UNBOUND，数据集+绑定→ENABLED

## 待实施

### Task 1: LabelService.update() 逻辑完善

**文件：** `profile-web/.../service/LabelService.java` L173-191

当前问题：
- L177: `Objects.equals(label.getSourceType(), 2)` 硬编码魔法数字
- 缺少标签存在性校验
- 缺少解绑回退状态逻辑

改造内容：
- 硬编码 `2` → `LabelSourceType.DATASET.getCode()`
- 新增标签存在性校验（不存在则抛异常）
- 状态自动管理：
  - 数据集类型 + 有 datasetFieldName → 绑定字段 + `ENABLED`
  - 数据集类型 + 无 datasetFieldName → `UNBOUND`（解绑回退）
  - 非数据集类型 → 不处理状态（保持原值）
- 不允许前端通过 update 修改 labelStatus（忽略前端传入值）

### Task 2: LabelService.delete() 枚举统一

**文件：** `profile-web/.../service/LabelService.java` L198-223

- L205: `SourceType.BUILT_IN` → `LabelSourceType.BUILT_IN`
- L215: `Objects.equals(label.getSourceType(), 2)` → `LabelSourceType.DATASET.getCode()`

### Task 3: Controller 瘦身 + MyBatis JOIN 优化

**目标：** Controller 只做纯 Converter 转换，entity info 通过 SQL JOIN 一次查出，消除 N+1

**文件：**
- `profile-web/.../controller/LabelController.java`
- `profile-web/.../mybatis/mapper/LabelMapper.xml`
- `profile-web/.../model/Label.java`
- `profile-web/.../service/LabelService.java`

**3a. MyBatis XML 增加 JOIN：**
- `selectByParams` 和 `selectByLabelId` 查询 LEFT JOIN `profile_meta_entity_identifier`（取 entity_identifier_name）和 LEFT JOIN `profile_meta_entity`（取 entity_name）
- Label ResultMap 新增 `entityIdentifierName`、`entityName` 映射

**3b. Label Model 新增 transient 字段：**
- `entityIdentifierName`、`entityName`（非持久化，由 JOIN 查出）

**3c. LabelController 彻底移除 populate 方法：**
- 移除 `populateEntityInfo`（列表 + 单条）
- 移除 `populateDatasetInfo`
- `getList()` 和 `getDetail()` 只做 Converter 转换

**3d. LabelService.getDetail() 简化：**
- 移除 entity identifier 二次查询（L90-96），JOIN 已查出
- 保留 dataset field 查询（L97-102，dataset_field 表不适合 JOIN）

### Task 4: LabelRequest 字段精简

**文件：** `profile-web/.../dto/LabelRequest.java`

移除前端不应传入的字段：
- `isValid` — 后端自动设置
- `labelStatus` — 后端根据业务规则自动管理
- `creator` — 后端从 UserContext 获取
- `modifier` — 后端从 UserContext 获取
- `gmtCreate` — 后端自动设置
- `gmtModified` — 后端自动设置
- `labelId` — 创建时后端生成，更新时从路径参数获取

保留字段：业务字段 + `owner` + `datasetId` + `datasetFieldName`

### Task 5: 前端不再传入 label_status

**文件：**
- `profile-ui/src/views/label/create-datasource.vue` L473 — 移除 `label_status: skipSource.value ? 0 : 1`
- `profile-ui/src/views/label/create-custom.vue` L405 — 移除 `label_status: 1`

### Task 6: 标签列表页新增状态和负责人筛选

**现状分析：**
- 后端 `LabelParam` 已有 `labelStatus`，MyBatis 已支持 `labelStatus` 查询
- 后端 `LabelParam` 缺少 `owner` 字段，MyBatis 缺少 `owner` 过滤条件
- 前端 `LabelQueryParams` 已有 `label_status`，但无 UI 控件
- 前端无 `owner` 筛选字段

**后端改造：**
- `LabelParam.java` 新增 `owner` 字段
- `LabelMapper.xml` selectByParams 新增 `owner` 过滤条件

**前端改造：**
- `types/index.ts` `LabelQueryParams` 新增 `owner?: string`
- `index.vue` 筛选栏新增：
  - 标签状态下拉（el-select）：全部 / 未绑定(0) / 已启用(1) / 已停用(2)
  - 负责人下拉（el-select）：调用 `userApi.getList()` 获取用户列表
- 筛选变更时触发 `handleSearch` 重新查询

### Task 7: 编译验证与 Review

- `mvn clean compile` 后端
- `npx vue-tsc --noEmit` 前端
- Review 状态流转逻辑完整性
